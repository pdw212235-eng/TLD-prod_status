# 전기공사 프로젝트 표준 프로세스 — 진행 관리

`process.html` 은 사내 표준 문서 *「전기공사 프로젝트 표준 프로세스 v0.1」* 을
그대로 화면으로 옮긴 것으로, 읽는 문서인 동시에 프로젝트별 진행 대장이다.

설치·웹서버·인터넷이 필요 없다. **파일 서버의 공유 폴더에 두고 각자 열어 쓰면 된다.**
데이터는 그 폴더 안의 파일에 저장되므로, 브라우저 방문기록·쿠키를 지워도 사라지지 않는다.

---

## 1. 설치 (한 번)

파일 서버에 폴더를 하나 만들고 `process.html` 을 넣는다.

```
\\fileserver\공사\전기공사_표준프로세스\
    process.html               ← 이 파일을 연다
    TLD-process-data.json      ← 자동 생성 (데이터 원본)
    TLD-process-log.csv        ← 자동 생성 (활동 로그)
    _backup\                   ← 자동 생성 (일자별 스냅샷)
```

사용자는 `process.html` 바로가기를 바탕화면에 두고 더블클릭한다.
**Chrome 또는 Edge** 로 열어야 한다(폴더 저장 기능 때문).

---

## 2. 최초 1회 설정 — 폴더 연결

1. 페이지를 열면 상단에 노란 배너 **"폴더가 연결되지 않았습니다"** 가 보인다.
2. 헤더의 **폴더 연결** 을 누르고, `process.html` 이 들어 있는 그 폴더를 지정한다.
3. 브라우저가 쓰기 권한을 물으면 허용한다.
4. 배너가 초록색 **"폴더에 저장 중"** 으로 바뀌면 끝이다. 이후 모든 입력이 자동 저장된다.
5. 상단 **작성자** 칸에 이름을 넣는다. 모든 기록에 이 이름이 남는다.

> **브라우저를 새로 켜면 폴더 권한이 풀린다.** 브라우저 보안 정책이라 우회할 수 없다.
> 배너의 **폴더 다시 연결** 을 한 번 누르면 이전에 지정한 폴더를 그대로 이어 쓴다
> (폴더를 다시 고를 필요 없다). 데이터는 폴더 파일에 있으므로 유실되지 않는다.

---

## 3. 할 수 있는 것

- **8단계 섹션** — 단계별 목적 · 체크리스트 · 흔한 실패 · Gate(통과 기준)
- **체크리스트 63항목**, **산출물 36종**(상태 / 담당자 / 기한 / 완료일 / 비고)
- **산출물 링크 등록** — 한 산출물에 링크를 **여러 개** 등록할 수 있다.
  견적서 Rev.1 / Rev.2, 월별 작업일보, 시험 항목별 성적서처럼 문서가 늘어나는 항목에 쓴다.
  - `https://…` → 클릭하면 새 창으로 열린다
  - `\\fileserver\공사\…`, `D:\공사\…` → 브라우저가 직접 열 수 없으므로
    **복사** 버튼으로 주소를 복사해 탐색기 주소창에 붙여 넣는다
- **Gate 자동 판정** — 필수 산출물이 모두 완료이고 체크가 전부 채워지면 통과로 바뀐다
- **전체 현황** — 모든 프로젝트 × 8단계 진행률을 한 표로. 빠진 프로젝트를 찾는 화면
- **활동 이력** — 누가 언제 무엇을 체크했고 어떤 문서를 등록했는지. 검색·필터·CSV 내려받기
- **인쇄 / PDF** — 접힌 단계까지 펼쳐 출력. 준공도서 첨부 체크리스트로 쓸 수 있다

---

## 4. 로그 파일 — `TLD-process-log.csv`

체크·상태변경·링크등록/삭제·담당자/기한/완료일/비고 수정·프로젝트 등록/수정/삭제가
**일어날 때마다 한 줄씩 파일 끝에 덧붙는다.** 지우거나 고쳐 쓰지 않는다.

| 일시 | 작성자 | 프로젝트 | 단계 | 항목 | 동작 | 상세 |
|---|---|---|---|---|---|---|
| 2026-09-05 09:12:03 | 박대웅 | OO빌딩 로비 LED | 1 | 체크리스트 | 체크 | 인입 전원: 수전 방식… |
| 2026-09-05 09:14:21 | 박대웅 | OO빌딩 로비 LED | 1 | 현장조사 결과서 | 상태변경 | 미착수 → 완료 |
| 2026-09-05 09:14:40 | 박대웅 | OO빌딩 로비 LED | 1 | 현장조사 결과서 | 링크등록 | 현장조사서 v1 \| https://… |

- UTF-8 BOM 이 붙어 있어 **엑셀에서 더블클릭하면 한글이 깨지지 않는다.**
- 파일이 4MB 를 넘으면 `_backup\log-YYYY-MM-DD.csv` 로 자동 보관하고 새로 시작한다.
- 같은 내용의 최근 500건이 `TLD-process-data.json` 에도 함께 들어 있어,
  CSV 가 손상돼도 화면의 **활동 이력 → CSV 내려받기** 로 되살릴 수 있다.

---

## 5. 여러 명이 같이 쓸 때

각자 자기 PC에서 열되 **같은 공유 폴더를 지정**하면 된다. 서로의 입력을 지우지 않는다.

- 저장할 때마다 폴더의 최신 내용을 다시 읽어 **항목 단위로 합친다.**
  파일을 통째로 덮어쓰지 않으므로, 내가 1단계를 만지는 동안 남이 5단계를 만져도 둘 다 남는다.
- 체크는 **체크 하나하나에 시각이 붙어** 있어 개별로 합쳐진다.
- 15초마다 폴더를 확인해 남의 변경을 가져오고, `○○ 님의 변경 N건을 반영했습니다` 로 알린다.
  (입력 중일 때는 화면을 갈아 끼우지 않는다.)
- 프로젝트를 삭제하면 삭제 표시가 남아, 다른 PC 와 합칠 때 되살아나지 않는다.

**한계 — 알고 쓰는 게 낫다**

| 상황 | 결과 |
|---|---|
| 두 사람이 **같은 산출물의 같은 칸**을 동시에 고침 | 나중에 저장된 쪽이 남는다 (활동 이력에 양쪽 다 기록됨) |
| 정확히 같은 순간 로그가 겹침 | 아주 드물게 CSV 한 줄이 겹칠 수 있다. 데이터 JSON 의 사본으로 복구 가능 |
| 네트워크 드라이브가 끊김 | 배너에 `마지막 저장 실패` 표시. 입력은 브라우저에 남아 있다가 다시 연결되면 반영된다 |
| 저장하지 않은 변경이 있는 채로 창을 닫으려 함 | 브라우저가 한 번 물어본다 |

동시 사용이 잦은 프로젝트라면 **단계별 담당자를 나눠 두는 것**이 가장 확실하다.

---

## 6. 백업과 복구

- **자동** — 하루 첫 저장 때 `_backup\data-YYYY-MM-DD.json` 스냅샷. 30일치 유지, 오래된 것은 자동 삭제.
- **수동** — 헤더 **내보내기** 로 `TLD-process-data-YYYY-MM-DD.json` 을 언제든 내려받는다.
- **복구** — **불러오기** 로 그 JSON 을 고르면 현재 데이터에 **합친다**(덮어쓰지 않는다).
  같은 항목은 나중에 수정된 쪽이 남는다.
- 파일 서버 자체 백업(스냅샷/섀도복사)이 돌고 있다면 그것도 그대로 보호막이 된다.

---

## 7. 알아둘 제약

- **Chrome / Edge 만 폴더 저장을 지원한다.** Firefox·Safari 에서는 브라우저 저장 + 내보내기만 된다.
- 폴더 선택 창이 `process.html` 위치를 자동으로 찾아 주지는 않는다. 처음 한 번은 직접 찾아가야 한다.
- 브라우저를 새로 켤 때마다 **폴더 다시 연결** 클릭 1회가 필요하다 (보안 정책).
- 브라우저 저장소는 **폴더가 끊겼을 때를 위한 예비 사본**일 뿐이다. 원본은 폴더 안의 파일이다.

---

## 8. 표준 문서가 개정되면

`process.html` 의 `STAGES` 배열만 고치면 화면·목차·진행률·마스터 체크리스트가 모두 따라온다.

- **산출물 `id`(`d1a`, `d2b` …)는 저장 키이므로 바꾸지 않는다.** 항목이 늘면 새 id 를 붙인다.
- 체크 항목 id 는 위치에서 자동으로 만들어지므로, 항목 순서를 바꾸면 그 단계의 체크만 초기화된다.
- 고친 `process.html` 을 공유 폴더에 덮어쓰면 된다. 데이터 파일과 로그는 그대로 유지된다.
