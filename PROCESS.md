# 전기공사 프로젝트 표준 프로세스 — 진행 관리 페이지

`process.html` 은 사내 표준 문서 *「전기공사 프로젝트 표준 프로세스 v0.1」* 을
그대로 화면으로 옮긴 것으로, 읽는 문서인 동시에 프로젝트별 진행 대장으로 쓴다.

## 무엇을 할 수 있나

- **8단계 섹션** — 단계별 목적 · 체크리스트 · 흔한 실패 · Gate(통과 기준)
- **체크리스트** — 63개 항목을 프로젝트별로 체크. 새로고침해도 남는다.
- **산출물 36종** — 상태(미착수/진행중/검토중/완료/해당없음), 담당자, 기한, 완료일, 비고
- **산출물 링크 등록** — 한 산출물에 링크를 **여러 개** 등록할 수 있다.
  견적서 Rev.1 / Rev.2, 월별 작업일보, 시험 항목별 성적서처럼 문서가 늘어나는 항목에 쓴다.
  `https://…` 주소는 새 창으로 열리고, `\\nas\projects\…` 같은 사내 경로는
  브라우저가 직접 열 수 없으므로 **복사** 버튼으로 탐색기에 붙여 넣는다.
- **Gate 판정** — 그 단계의 필수 산출물이 모두 완료이고 체크리스트가 전부 채워지면 통과로 바뀐다.
- **전체 현황** — 모든 프로젝트 × 8단계 진행률을 한 표로 본다. 빠진 프로젝트를 찾는 화면이다.
- **인쇄 / PDF** — 접힌 단계까지 모두 펼쳐서 출력된다. 준공도서 첨부용 체크리스트로 쓸 수 있다.

## 배포 (최초 1회)

`index.html` 과 같은 Apps Script 웹앱·같은 스프레드시트를 그대로 쓴다.

1. Apps Script 편집기에서 `Code.gs` 전체를 이 저장소의 최신 내용으로 교체한다.
2. `setupProcessSheets` 함수를 한 번 실행한다.
   스프레드시트에 `ProcessProjects`, `ProcessItems` 두 시트가 만들어진다.
   (실행하지 않아도 첫 저장 시 자동 생성된다.)
3. **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포** 로 재배포한다.
   기존 배포를 수정하면 URL 이 그대로라 프론트엔드는 손댈 필요가 없다.
   새 배포를 만들었다면 `index.html` 과 `process.html` 의 `GAS_URL` 을 둘 다 바꾼다.

재배포 전이거나 서버에 닿지 못하면 페이지 상단에 **로컬 저장 모드** 배너가 뜨고,
입력은 그 브라우저에만 저장된다(공유되지 않는다). 배너가 없으면 정상 공유 상태다.

## 권한

| 동작 | 권한 |
|------|------|
| 프로젝트 · 진행상황 조회 | 누구나 |
| 프로젝트 등록 | 누구나 |
| 체크 · 상태 · 링크 등록/삭제 | 누구나 |
| 프로젝트 삭제 (`proc-project-delete`) | 관리자 |

누가 무엇을 등록했는지 남기려고 상단 **작성자** 칸의 이름을 각 링크와 시트의 `수정자` 열에 함께 기록한다.
이름은 브라우저에 기억되므로 한 번만 입력하면 된다.

## 데이터 구조

**ProcessProjects**

`id` · `프로젝트명` · `발주처` · `현장` · `착수일` · `준공목표일` · `PM` · `등록일시` · `등록자`

**ProcessItems** — (프로젝트 × 항목) 1행

`rowId` · `프로젝트` · `항목ID` · `단계` · `항목명` · `상태` · `담당자` · `기한` · `완료일` · `링크JSON` · `비고` · `수정일시` · `수정자`

- `링크JSON` 은 `[{id,label,url,by,at}, …]` 배열이다.
- 항목ID `__checks` 행은 그 프로젝트의 체크리스트 상태를 `링크JSON` 에 체크된 id 배열로 담는다.

## 표준 문서가 개정되면

`process.html` 의 `STAGES` 배열만 고치면 화면·목차·진행률·마스터 체크리스트가 모두 따라온다.
**산출물 `id`(`d1a`, `d2b` …)는 저장 키이므로 바꾸지 않는다.** 항목이 늘면 새 id 를 붙인다.
체크 항목 id 는 위치에서 자동으로 만들어지므로, 항목 순서를 바꾸면 그 단계의 체크만 초기화된다.
