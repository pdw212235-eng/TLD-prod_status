# 전기공사 프로젝트 표준 프로세스 — 진행 관리 (오프라인)

`process.html` 은 사내 표준 문서 *「전기공사 프로젝트 표준 프로세스 v0.1」* 을
그대로 화면으로 옮긴 것으로, 읽는 문서인 동시에 프로젝트별 진행 대장으로 쓴다.

**설치·서버·인터넷이 필요 없다.** 파일 하나를 내 PC에 두고 더블클릭해서 연다.
외부 스크립트도 폰트도 불러오지 않으므로 인터넷이 끊긴 상태에서도 그대로 동작한다.

## 쓰는 법

1. `process.html` 을 PC의 아무 폴더에나 저장한다. (예: `바탕화면\전기공사\`)
2. 더블클릭 — Chrome 또는 Edge 로 열린다.
3. 상단 **작성자** 칸에 이름을 한 번 넣는다. 등록한 링크에 이름이 함께 남는다.
4. **＋ 프로젝트** 로 프로젝트를 만들고, 단계를 따라가며 체크하고 산출물 링크를 등록한다.

즐겨찾기에 등록해 두면 다음부터는 클릭 한 번으로 열린다.

## 할 수 있는 것

- **8단계 섹션** — 단계별 목적 · 체크리스트 · 흔한 실패 · Gate(통과 기준)
- **체크리스트 63항목** — 프로젝트별로 저장된다
- **산출물 36종** — 상태(미착수/진행중/검토중/완료/해당없음), 담당자, 기한, 완료일, 비고
- **산출물 링크 등록** — 한 산출물에 링크를 **여러 개** 등록할 수 있다.
  견적서 Rev.1 / Rev.2, 월별 작업일보, 시험 항목별 성적서처럼 문서가 늘어나는 항목에 쓴다.
  - `https://…` (구글드라이브, 사내 웹) → 클릭하면 새 창으로 열린다
  - `\\nas\projects\…`, `D:\공사\…` 같은 경로 → 브라우저가 직접 열 수 없으므로
    **복사** 버튼으로 주소를 복사해 탐색기 주소창에 붙여 넣는다
- **Gate 자동 판정** — 그 단계의 필수 산출물이 모두 완료이고 체크가 전부 채워지면 통과로 바뀐다
- **전체 현황** — 모든 프로젝트 × 8단계 진행률을 한 표로 본다. 빠진 프로젝트를 찾는 화면이다
- **인쇄 / PDF** — 접힌 단계까지 모두 펼쳐 출력된다. 준공도서 첨부 체크리스트로 쓸 수 있다

## 데이터가 저장되는 곳

데이터는 **이 PC 밖으로 나가지 않는다.** 어디에도 전송하지 않는다.

| 방식 | 설명 |
|------|------|
| 브라우저 저장소 | 기본값. 자동 저장되고 파일을 다시 열면 그대로 남아 있다. **브라우저 방문기록·쿠키를 지우면 함께 지워진다.** |
| **파일 연결** (Chrome/Edge) | 원하는 위치에 `TLD-process-tracker.json` 을 지정해 두면 변경할 때마다 그 파일에도 자동 기록된다. 이게 진짜 원본이다. **권장.** |
| **내보내기 / 불러오기** | 어느 브라우저에서나 되는 백업·이관 수단. 다른 PC로 옮길 때 쓴다. |

상단 배너가 지금 어디에 저장되고 있는지 항상 알려 준다.
초록색(파일에 저장 중)이면 안전하고, 노란색이면 브라우저 저장소만 쓰는 중이다.

### 처음 열었을 때 권장 설정

**파일 연결** → 저장 위치를 지정(예: `내문서\TLD-process-tracker.json`) → 끝.
이후로는 자동이다. 브라우저를 새로 열면 보안상 권한이 초기화되므로
배너의 **데이터 파일 다시 연결** 을 한 번 눌러 주면 그 파일을 이어서 쓴다.

### 다른 PC로 옮기거나 백업할 때

**내보내기** → 생긴 `TLD-process-tracker-YYYY-MM-DD.json` 을 옮긴다 →
받은 PC에서 `process.html` 을 열고 **불러오기**.

> 불러오기는 화면의 데이터를 파일 내용으로 **교체**한다. 합치지 않는다.
> 여러 사람이 각자 PC에서 쓰면 서로 덮어쓰게 되므로, 프로젝트별로
> 담당자 한 명이 관리하거나 공유 폴더의 JSON 한 개를 함께 쓰는 방식을 정해 두는 게 좋다.

## 표준 문서가 개정되면

`process.html` 의 `STAGES` 배열만 고치면 화면·목차·진행률·마스터 체크리스트가 모두 따라온다.

- **산출물 `id`(`d1a`, `d2b` …)는 저장 키이므로 바꾸지 않는다.** 항목이 늘면 새 id 를 붙인다.
- 체크 항목 id 는 위치에서 자동으로 만들어지므로, 항목 순서를 바꾸면 그 단계의 체크만 초기화된다.

고친 파일을 다시 배포할 때 기존 데이터는 그대로 남는다
(브라우저 저장소와 JSON 파일은 HTML 과 분리돼 있다). 다만 파일 이름과 위치를
바꾸지 않는 편이 안전하다.
